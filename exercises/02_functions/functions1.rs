// TODO: Add some function with the name `call_me` without arguments or a return value.

use std::fmt::Error;

fn main() {
    call_me(); // Don't change this line
}

fn call_me() -> Result<(), Error> {
    Ok(())
}